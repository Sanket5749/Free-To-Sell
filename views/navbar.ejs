<nav class="navbar navbar-expand-lg  navbar-dark bg-dark shadow-lg py-3">
  <div class="container">
    <button onclick="myFunction()" class="btn-tog mx-3"><i class="fa-solid fa-moon"></i></button>
    <a class="navbar-brand fw-bold text-light fs-4" href="/">Free To Sell</a>
    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon text-light"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (!currentUser) { %>
        <li class="nav-item">
          <a class="nav-link px-3 fw-semibold text-light hover-effect" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="btn btn-warning text-dark rounded-pill px-4 py-2 shadow-lg" href="/signup">Sign Up</a>
        </li>
        <% } %>
        <% if (currentUser) { %>
        <li class="nav-item">
          <a class="nav-link px-3 fw-semibold text-light hover-effect" href="/listings/new">New Product</a>
        </li>
        <li class="nav-item">
          <form action="/logout" method="POST" class="m-0">
            <button class="btn btn-warning rounded-pill px-4 py-2 shadow-lg">Logout</button>
          </form>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<script>
  function myFunction() {
  var element = document.body;
  element.classList.toggle("dark-mode");
}
</script>